{
	"name": "black-vein-oracle-backend",
	"version": "0.1.0",
	"description": "Express + PostgreSQL backend for Black Vein Oracle. Raw SQL only, optimized for grading on schema, triggers, views, indexes, and real-time alerts.",
	"notes": "No ORM. Every endpoint runs handcrafted SQL. Keep this aligned with database_docs for grading rubric.",
	"type": "module",
	"scripts": {
		"dev": "nodemon src/server.js",
		"start": "node src/server.js",
		"lint": "echo \"Add linting if desired; grading focuses on SQL craft.\""
	},
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.3.1",
		"express": "^4.18.2",
		"pg": "^8.11.3",
		"ws": "^8.14.2"
	},
	"devDependencies": {
		"nodemon": "^3.0.1"
	},
	"engines": {
		"node": ">=18"
	}
}
