# Backend environment template for Black Vein Oracle.
# This file exists so graders know exactly which knobs control the bleeding database.
# Copy to .env and fill with real secrets before running.

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=black_vein_oracle
# Pool sizing tuned for EXPLAIN ANALYZE-heavy workloads during grading
PGPOOL_MIN=2
PGPOOL_MAX=20
PGPOOL_IDLE_TIMEOUT=10000

# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# WebSocket channel name used by triggers and LISTEN/NOTIFY
ALERT_CHANNEL=incident_alerts
